/* module.exports = (sequelize, dataTypes) =>{
    let alias = "Actor";
    let cols = {

    };
    let config = {

    };

    const Actor = sequelize.define(alias, cols, config);
    return Actor;
} */